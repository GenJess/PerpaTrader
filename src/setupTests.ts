// This file can be used for global test setup.
// Import jest-dom matchers for Vitest
import '@testing-library/jest-dom/vitest';

// You can also set up global mocks here if needed for multiple test files.
// For example:
// vi.mock('some-module', () => ({
//   default: vi.fn(),
// }));
